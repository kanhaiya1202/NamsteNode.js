const {additon} = require('./sum');
const {multiplication}= require('./multi');
const {division} = require('./div');
const {subtraction} = require('./sub');

module.exports = {additon,multiplication,division,subtraction}